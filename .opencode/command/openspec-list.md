---
description: Show a compact OpenSpec change and accepted-spec index.
agent: build
---

Run `openspec list --changes` and `openspec list --specs` from the repository
root and return the compact output. Do not read individual OpenSpec proposal,
design, task, or spec files. Use this for an inventory only. If the user asks
for details about one entry, inspect only that entry afterward.
